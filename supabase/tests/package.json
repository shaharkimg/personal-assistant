{
  "name": "personal-assistant-db-tests",
  "private": true,
  "type": "module",
  "description": "Runs the migration against PGlite (Postgres + pgvector in WASM) and verifies RLS isolation.",
  "scripts": { "test": "node rls.test.mjs" },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@electric-sql/pglite-pgvector": "^0.0.9"
  }
}
